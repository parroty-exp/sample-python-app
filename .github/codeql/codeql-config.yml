name: "CodeQL config"
disable-default-queries: true
queries: 
#  - uses: security-extended
#  - uses: security-and-quality
  - uses: ./code-queries/my-custom-suites/sample.ql
paths-ignore:
  - tests
  - lib
